{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/mosaicism/master/assets/schema_input.json",
    "title": "nf-core/mosaicism pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "bam": {
                "type": "string",
                "pattern": "^\\S+\\.bam$",
                "errorMessage": "BAM file must be provided and cannot contain spaces"
            },
            "bai": {
                "type": "string",
                "pattern": "^\\S+(\\.bam)?\\.bai$",
                "errorMessage": "BAI file must be provided and cannot contain spaces"
            },
            "bed": {
                "type": "string",
                "pattern": "^\\S+\\.bed$",
                "errorMessage": "BED file must be provided and cannot contain spaces"
            }
        },
        "required": ["bam", "bai", "bed"]
    }
}
